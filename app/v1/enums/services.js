/**
 * @readonly
 * @enum {string}
 */
const SERVICES = {
    mysqlConnection: 'mysqlConnection',
    db: 'db',
    kindergartenService: 'kindergartenService',
};

module.exports = {
    SERVICES, // this enum have to be wrapped in object!
};
